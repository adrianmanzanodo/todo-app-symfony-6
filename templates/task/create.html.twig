{% extends 'layout.html.twig' %}

{% block title %}Create task{% endblock %}

{% block page_contents %}
    {% for task in tasks %}
        <h2>{{ task.name }}</h2>
        <p>{{ task.description }}</p>
    {% endfor %}
{% endblock %}